```bash
$ termux-setup-storage

• Enter • Ketik y Atau Pilih Izinkan

$ pkg update && pkg upgrade

$ pkg install python

$ pkg install git

$ pip install requests

$ pip install mechanize

$ pip install bs4

$ pip install futures

$ git clone https://github.com/TANTIERINA-XD/hejo4

$ cd hejo4

$ git pull

$ python hejo4.py
```
